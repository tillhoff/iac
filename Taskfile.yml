# https://taskfile.dev

version: '3'

silent: true

tasks:

  docker-compose:postgres:
    cmds:
      - docker-compose -d -f docker-compose-files/postgres-compose.yml up

  create-secret-files:
    cmds:
      - echo 'POSTGRES_USER=\nPOSTGRES_PASSWORD=' > secrets/postgres.ignored
