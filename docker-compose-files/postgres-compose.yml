version: "3.8"

services:
  db:
    image: postgres
    restart: always
    env_file:
      - secrets/postgres.ignored
    volumes:
    - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /mnt/volume/postgres
